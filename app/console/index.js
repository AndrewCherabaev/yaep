module.exports = function(program){
	/** TODO: Add custom commands if you wants so
		see https://github.com/tj/commander.js/blob/master/Readme.md

		use the following syntax:
		require('./commands/default_command')(program);
	 */

	require('./commands/show_true')(program);
}